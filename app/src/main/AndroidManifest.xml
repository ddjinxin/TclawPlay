<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 读取存储权限（扫描音乐和读取歌词文件） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Android 9及以下写公共目录（歌词复制到Download/lyrics/） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Android 11+读取Download目录下其他app创建的备份文件（WebDAV配置恢复） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- 前台服务权限（后台播放） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- 音频设置权限（Visualizer 获取频谱数据） -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- 网络访问权限（获取在线歌词和封面） -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- 悬浮窗权限（车机桌面悬浮应用） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JingxinMusic"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.JingxinMusic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 播放页面 -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.JingxinMusic" />
        
        <!-- WebDAV设置页面 -->
        <activity
            android:name=".ui.WebDavSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.JingxinMusic" />
        
        <!-- B站音源设置页面 -->
        <activity
            android:name=".ui.BiliSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.JingxinMusic" />
        
        <!-- 播放服务 -->
        <service
            android:name=".service.MusicPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        
        <!-- 悬浮迷你播放窗服务 -->
        <service
            android:name=".service.MiniFloatService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- FileProvider：允许其他应用通过 MediaSession 访问封面 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>
    
</manifest>